# 카탈로그·독서 UI 변경 (2026-09-19)

- 본문 사전 단어의 상시 밑줄을 제거했다. 호버 시 옅은 기존 계열 배경색과 포인터만 표시하고,
  클릭·Enter·Space로 설명을 연다. 포커스 링은 키보드 사용자를 위해 유지했다.
- 헤더의 이름은 페이지 링크 대신 프로필 팝오버를 연다. 안쪽 마이페이지 링크만 이동하며,
  로그아웃·외부 클릭·Escape·포커스 이동으로 닫힌다. Escape는 이름 버튼으로 포커스를 복원한다.
- 홈의 조회수 랭킹·별점 랭킹·오늘의 신작은 각각 실제 랭킹 API를 호출한다.
- 랭킹 페이지의 세 기준은 URL 쿼리와 동기화되며 장르 필터도 API로 전달한다.
- 홈 장르 링크와 장르 페이지는 실제 DB 장르를 사용한다. 작품 카드·상세는 DB 태그를 표시한다.
- 로딩·오류·재시도·빈 목록을 구분한다. 오늘의 신작이 없으면 실제 빈 상태를 표시한다.
- 320px에서 긴 닉네임과 가로 목록 화살표로 화면이 넘지 않도록 크기를 제한했다.

백엔드의 `CATALOG_REVIEW.md` 순서로 DB 컬럼 추가와 소개글 수정을 적용한 뒤 API를 배포해야 한다.
기존 필드는 호환되며, 프론트도 Vercel에서 이 변경을 포함한 커밋으로 다시 배포해야 화면이 바뀐다.
표시하는 조회수·별점은 DB의 기존 집계 값이다. 이번 변경에는 평가 제출 UI나 조회 이벤트 수집을 추가하지 않았다.

검증 명령:

```sh
npm run build
npm run lint
npm test
node tests/browser-catalog.mjs
node tests/browser-reader-assist.mjs
```

브라우저 스크립트는 Vite가 실행 중이어야 하며 `PLAYWRIGHT_MODULE`, `CHROME_PATH`, `BASE_URL`로
환경을 지정할 수 있다. API 응답을 고정한 격리된 브라우저에서 UI를 검사하며 운영 DB를 수정하지 않는다.

검증 결과: TypeScript/Vite 빌드, lint, 단위 테스트 22개 통과.
320/390/1280px 카탈로그 UI와 프로필 메뉴, 390/820px를 포함한 독서 보조 기능 검증이 통과했다.
