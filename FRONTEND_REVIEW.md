# 프론트엔드 반영 및 검토 — 2026-09-17

`origin/main`의 `3e526d4` (`design: 로그인 페이지 UI 디자인`)를 로컬 `main`에 fast-forward로 반영했다. 기존 툴팁 응답 정규화, 회차별 캐시, 진행률 저장, 재방문 모달 수정과 테스트는 그대로 보존했다.

## 발견 및 수정

- **긴 문단의 뒷부분이 잘림:** 새 페이지 모드는 문단 전체를 한 페이지에 넣었다. 390×844 화면에서 실제 본문 높이 2,908px를 659px 영역에 넣고 진행률을 100%로 표시하는 현상을 재현했다. 글꼴과 너비를 기준으로 긴 문단을 분할하고, 원문 오프셋·원래 문단의 툴팁 문맥·유니코드 문자 경계를 유지하도록 수정했다. 진행률과 이동 위치도 원문 위치를 사용하며, 웹폰트 로딩 및 화면 크기 변경 시 다시 계산한다.
- **모바일 검색창이 화면 밖으로 벗어남:** 작은 화면에서 헤더가 3열 구성을 유지해 검색창 왼쪽이 -127px까지 밀렸다. 모바일 헤더 열 구성과 검색창 배치를 수정했다.
- **검색 결과에 읽을 수 없는 샘플 작품이 섞임:** 검색은 실제 `/api/novels` 목록만 사용한다. 로딩·요청 실패·결과 없음 상태를 구분하고, 실패 시 재시도를 제공한다.
- **샘플 랭킹이 실제 순위처럼 보임:** 디자인은 보존하면서 홈·장르·랭킹에 예시 작품 안내를 추가했다. 실제 장르·랭킹 API 연결은 아직 구현되어 있지 않다.

## 검증

- `npm test`: 17개 단위 테스트 통과.
- `npm run build`: TypeScript 검사 및 Vite 프로덕션 빌드 통과.
- `npm run lint`, `git diff --check`: 통과.
- `tests/browser-review.mjs`: 8개 브라우저 시나리오 통과. 390×844, 1280×900, 844×390에서 본문 전체 보존, 툴팁 문맥, 회차 전환, 확대·줄간격·폰트·리사이즈를 확인했다. 320/390/768/1280px에서 검색창 위치, 실제 작품 검색, 샘플 제외, 장르·랭킹 이동을 확인했다. API 실패 표시도 검증했다.
- 기존 툴팁 브라우저 회귀 테스트 10개 통과: 인물·장소·사건·일반 단어, 응답 형식 변형, 빈 응답, 회차별 캐시.

이번 브라우저 검증은 실제 Chrome 렌더링과 통제된 API 응답을 사용했다. 실제 DB·OpenAI 호출을 다시 수행한 검증은 아니며, 이전 실환경 검증 기록은 백엔드의 `LIVE_VERIFICATION.md`에 있다. 배포는 수행하지 않았다.

## 브라우저 테스트 실행

Vite를 4173 포트로 실행한 뒤 Playwright를 사용할 수 있는 환경에서 실행한다. API 요청은 테스트가 가로채므로 백엔드 서버는 필요하지 않다.

```sh
npm run dev -- --host 127.0.0.1 --port 4173
# 별도 터미널 (playwright를 import할 수 있어야 함)
node tests/browser-review.mjs
```

다른 환경에서는 `BASE_URL`, `PLAYWRIGHT_MODULE` (공유 Playwright 모듈 경로), `CHROME_PATH` (Chrome 실행 파일 경로)를 지정할 수 있다.
